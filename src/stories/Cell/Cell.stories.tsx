import React from 'react';
import { Cell } from '../../components/Cell/Cell';

export default {
  title: 'Cell',
  component: Cell,
};

const Template = () => {
  return <Cell value="" />;
};
export const Default = Template;
