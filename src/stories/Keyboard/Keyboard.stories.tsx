import React from 'react';
import Keyboard from '../../components/Keyboard/Keyboard';

export default {
  title: 'Keyboard',
  component: Keyboard,
};

export const Default = () => {
  return <Keyboard />;
};
